import { BookSlide, FeaturedMagic, Header, Services } from 'components';
import React from 'react';

const Teams = () => {
  return (
    <div className="bg-dark">
      <Header isTransparent />
      <FeaturedMagic />
    </div>
  );
};

export default Teams;
